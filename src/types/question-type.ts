    export interface QuestionType {
        id: number;
        pertanyaan: string;
        kategori?: string;
    }

    export interface QuestionTypeLevel1Form {
        pertanyaan: string ;
        kategori: string ;
    }

    export interface QuestionTypeLevel3Form{
    pertanyaan: string;
    kategori: string;
    }